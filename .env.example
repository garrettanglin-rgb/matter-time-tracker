# Copy this file to .env and fill in your values.

# Anthropic API key (required)
ANTHROPIC_API_KEY=sk-ant-xxxxx

# Path to matters registry JSON (optional, defaults to ./matters_registry.json)
# MATTERS_REGISTRY_PATH=/absolute/path/to/matters_registry.json

# Directory for exported reports (optional, defaults to ./output)
# OUTPUT_DIR=/absolute/path/to/output

# Email time-estimation thresholds (optional).
# JSON array of [max_words, hours] pairs. Use null for the final catch-all.
# Default: under 100 words → 0.1h, 100-300 words → 0.2h, over 300 → 0.3h
# EMAIL_TIME_THRESHOLDS=[[100, 0.1], [300, 0.2], [null, 0.3]]
